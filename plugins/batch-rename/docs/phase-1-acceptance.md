# Phase 1 验收记录

## 当前状态

- 自动测试：6 项通过；类型检查、构建和安装包入口检查通过。
- ZTools 宿主入口：2026-09-25 用户反馈以下验收步骤全部通过。Phase 1 验收完成。
- 本阶段只读取文件信息和展示名称，不执行重命名。

## 安装

在 ZTools 插件管理页使用“本地安装”，选择 `outputs/batch-rename-phase1-0.1.0.zip`。本阶段插件名称为“批量重命名”，与此前的“批量重命名入口 Probe”是两个独立安装项；测试时请选择新插件。

## 宿主验收步骤

请使用临时测试文件，并在测试前记下其原始名称。

1. 在 Explorer 选中 4 个文件，经 ZTools 文件上下文打开“批量重命名”。预期：列表恰好显示 4 个文件，按名称升序排列，当前名称和目标名称一致。
2. 保持 Explorer 选择，经 `Alt+Z` 搜索“批量重命名”并进入。预期：原有列表保持，不重复添加文件。
3. 在 Explorer 选中另一批文件，再经文件上下文进入。预期：列表替换为新文件，不包含上一批文件。
4. 点击单行 `×`、再点击“清空”。预期：数量和列表即时更新；Explorer 中的文件仍存在，文件名与测试前一致。

验收结果：用户确认以上步骤均无问题，测试通过。
