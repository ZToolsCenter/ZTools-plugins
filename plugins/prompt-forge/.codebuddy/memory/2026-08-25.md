# 2026-08-25

## 点击提示词的条件显示逻辑
- 需求：点击提示词时判断是否含变量；无变量隐藏「填写变量」区域，全部空间展示内容；有变量正常显示填写页面
- 实现：改造 `FillPanel.vue` 自适应布局
  - 左侧表单 `div.fill-form` 加 `v-if="variables.length > 0"`，无变量时隐藏
  - `.fill-body` 加动态 class `single`（`variables.length === 0` 时），切换为单列 grid `grid-template-columns: 1fr`
  - 移除不再引用的 `.no-vars` 死样式
- 判断逻辑：`variables = computed(() => props.unit?.variables || [])`，准确识别变量存在性（含 undefined 兜底）
- 点击流程不变：`PromptList @activate` → `selectActive()` → `phase='fill'` → FillPanel 自适应
- 构建通过（80 模块）

## 备注
- 键盘 Enter 键对无变量提示词仍为「直接复制」（未改动），点击则进入纯预览模式，两者行为有差异但均合理

## Markdown 渲染预览（方案 A：视图切换按钮）
- 依赖：新增 `markdown-it` + `@types/markdown-it`
- 新增 `src/utils/markdown.ts`：封装渲染实例（`html: false` 防 XSS，`linkify: true`，`breaks: false`）
- `FillPanel.vue`：预览区加「文本 / Markdown」切换（`previewMode` ref），Markdown 模式用 `v-html` 渲染（先 `renderVariables` 替换变量，再 `renderMarkdown`）
- `ManageContentTab.vue`：正文区加「编辑 / 预览」切换（`bodyMode` ref）
- 样式抽取：`.mode-toggle` / `.mode-btn` 切换按钮 + `.markdown-body` 全套元素样式（h1-h6/p/ul/code/pre/blockquote/table 等）放入全局 `src/main.css`，供两组件共享
- FillPanel 仅保留 `.preview-body.markdown-body` 覆盖（字体/white-space），因 scoped `.preview-body` 特异性高于全局 `.markdown-body`
- 构建通过（101 模块，JS 174KB→290KB，markdown-it 体积）
- 代码块语法高亮（highlight.js）暂未做，保持轻量

## 使用统计分析面板（仪表盘）
- 新增 `src/components/StatisticsPanel.vue`，入口在 SpaceView 侧栏「统计」tab（📊）
- 图表全部手写 SVG/CSS，未引入图表库（保持轻量）：
  - 4 个统计卡片：总提示词数、累计使用次数、收藏数、含变量数
  - 类型分布环形图（SVG circle + stroke-dasharray 实现 donut，4 类型配色）
  - 使用 TOP10（本周/本月切换，横向条形，基于 historyItems 按 promptId 聚合）
  - 使用频次趋势折线图（SVG polyline + 面积填充 + 数据点 + 日期标签，按日期聚合）
- 时间范围 `range: 'week' | 'month'`（7/30 天）同时驱动 TOP10 与趋势图
- `prompt.ts`：`spaceTab` 类型扩展加 `'stats'`；SpaceView 统计 tab 隐藏顶栏（`v-if="spaceTab !== 'stats'"`）
- 关键点：趋势与 TOP10 都基于 `historyItems`（有 usedAt 时间戳），而非 usageCount（无时间维度）
- 构建通过（104 模块）

## UI 图标替换：emoji → lucide-vue-next
- 新增依赖 `lucide-vue-next`（Lucide 线条图标库，与项目现有内联 SVG 风格一致，tree-shakeable）
- 替换范围（约 11 个文件）：
  - SpaceSidebar 侧栏 8 图标（List/Clock/Star/History/Folder/Package/BarChart3/Trash2）
  - PromptList：日历 Calendar、收藏星 Star、右键菜单 Pencil/Trash2
  - ProjectPanel 右键菜单：FolderInput/Trash2/Check/ArrowLeft
  - ComposeView：Pin/Shield/Paperclip/ChevronUp/ChevronDown/X
  - SettingsView：Settings/Palette/Download/Info
  - WizardView：Check/Zap/ArrowLeft/ArrowRight
  - 其余：ShortcutPanel X、ManageVersionsTab X、ManagePropsTab Star、ManageView Star、HistoryPanel X、QuickSaveView TriangleAlert/X
- 收藏星实心/空心：`<Star :fill="item.favorite ? 'currentColor' : 'none'" />`
- 图标用法：`<component :is="nav.icon" :size="16" />`（数据驱动侧栏）
- 保留通知/alert 文本里的 ✓ ⚠（非 UI 图标）
- 构建通过（1818 模块，JS 仅 +14KB，tree-shaking 生效）
- 注意：lucide-vue-next 新版警告图标名为 `TriangleAlert`（旧名 AlertTriangle）
