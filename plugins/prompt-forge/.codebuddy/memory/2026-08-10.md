# 2026-08-10

## D4 持久化 Debounce
- `prompt.ts` 新增 `schedulePersist()` 300ms debounce + `flushPersist()` 立即写入
- 高频操作（toggleFavorite/softDelete/restore/hardDelete/addItem/updateItem）改用 debounce
- `recordUsage` 保留立即写入（用户复制后使用计数需实时）
- 批量操作保留 `persistAll()` 立即写入

## A4 搜索高亮
- `PromptList.vue` 新增 `query` prop + `highlightText()` 函数
- 标题用 `v-html` 渲染，搜索词用 `<mark>` 高亮
- 浅色主题黄色底 / 深色主题橙色底
- `SpaceView.vue` 两处 PromptList 传入 query

## B1 版本差异对比
- `ManageVersionsTab.vue` 新组件，内聚 LCS-based 行级 diff 算法
- 每个历史版本新增"对比"按钮
- 全屏覆盖层展示 unified diff：绿色(+)/红色(-)/白色(不变)
- diff 算法合并相邻同类型行减少碎片

## A1 全局快捷键面板
- `ShortcutPanel.vue` 新组件，按 `?` 弹出
- 分组显示：空间搜索模式(4项) + 填写模式(2项) + 全局(1项)
- `Esc` 或点击遮罩关闭
- `SpaceView.vue` handleKeyDown 集成

## D2 组件拆分重构
- SpaceView(608行→210行)：拆出 SpaceSidebar(80行) / ProjectPanel(130行) / HistoryPanel(90行) / TrashPanel(60行)
- ManageView(477行→280行)：拆出 5 个 Tab 组件
- 共新增 10 个组件文件，净减 ~300 行

## 第二批优化：Bug 修复 + 缓存优化
- F1: FillPanel.vue `v-for="t in unit.tags.slice(0,3)"` → `(unit?.tags || []).slice(0,3)`，防 null 崩溃
- F2: ComposeView `onMounted` 中 `basePrompts.value[0].id` 加空值守卫
- F3: storage.ts fallback 路径 key 前缀 `pf:` → `promptforge:`，与 preload services.js 统一
- F8: prompt.ts + ManageView.vue Fuse 索引从 `computed(new Fuse(...))` 改为 `ref + watch` 懒加载，仅在查询非空时按需构建
