/**
 * 定时任务运行前提文案（uTools 插件生命周期约束）。
 * 列表页头 / 空态 / 列表底栏共用；表单不再堆说明。
 */

/** 一行短提示（列表页头、列表底栏） */
export const AUTOMATION_RUNTIME_HINT_SHORT =
  "需插件在运行；请在 uTools 启用「跟随主程序同时启动运行」";

/** 列表空态 */
export const AUTOMATION_RUNTIME_NOTE_EMPTY =
  "任务仅在插件运行时执行。请在 uTools 启用「跟随主程序同时启动运行」。";
