name: Build Release Plugin Platform

on:
  workflow_call:
    inputs:
      platform_name:
        required: true
        type: string
      runner:
        required: true
        type: string

jobs:
  build:
    runs-on: ${{ inputs.runner }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.5.0

      - name: Download build info
        uses: actions/download-artifact@v4
        with:
          name: release-build-info
          path: release

      - name: Select platform build info
        run: |
          node -e "const fs = require('fs'); fs.copyFileSync('release/build-info.${{ inputs.platform_name }}.json', 'release/build-info.json')"

      - name: Detect build requirements
        id: build-requirements
        run: node scripts/detect-build-requirements.js

      - name: Setup Bun
        if: steps.build-requirements.outputs.needs_bun == 'true'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: npm install

      - name: Build plugins
        run: node scripts/build-plugins.js

      - name: Upload plugin packages
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ inputs.platform_name }}-plugin-packages
          path: release/*.zip
          if-no-files-found: error
